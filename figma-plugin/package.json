{
  "name": "uspec-extract",
  "version": "2.1.0",
  "private": true,
  "description": "Figma plugin that walks a selected component + its sub-components and emits the complete _base.json the create-component-md skill chain consumes.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:watch": "node scripts/build.mjs --watch",
    "validate": "node scripts/validate-base.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.109.0",
    "ajv": "^8.17.1",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
